# Verification Checklist

## 页面和路由
- [x] AI+EE 二级目录页面在 `/thinking/ai-ee` 路径可访问
- [x] 从 Thinking 板块点击 AI+EE 卡片的"Read More"能正确跳转到二级目录
- [x] 页面完全复用主站设计语言（CSS 变量、字体、间距系统）
- [x] 页面有返回主页的导航按钮

## 卡片展示（圆角风格）
- [x] 所有 AI+EE idea 卡片使用 `var(--radius-xl)` 或 `var(--radius-2xl)` 圆角
- [x] 卡片采用网格布局（桌面端 3 列，平板 2 列，移动端 1 列）
- [x] 卡片默认使用 `--shadow-md` 阴影，悬停时使用 `--shadow-lg`
- [x] 卡片悬停时上浮 8px (`transform: translateY(-8px)`)
- [x] 卡片悬停时有热浪涟漪效果（径向渐变从中心扩散）
- [x] 卡片悬停过渡时间为 300ms (`var(--transition-base)`)
- [x] 每个卡片显示标题、内容摘要和灯泡图标（橙色渐变背景）
- [x] 卡片点击时有 `scale(0.95)` 的视觉反馈

## 模态框功能（动态交互）
- [x] 点击卡片能打开模态框查看详情
- [x] 模态框显示完整的 idea 内容
- [x] 模态框使用 `var(--radius-2xl)` 圆角
- [x] 模态框背景遮罩为半透明黑色 `rgba(0,0,0,0.5)`
- [x] 模态框打开/关闭动画使用 `var(--transition-spring)` (600ms cubic-bezier)
- [x] 模态框有淡入淡出 + 缩放的复合动画效果
- [x] 模态框右上角有关闭按钮（圆形，悬停有颜色变化）
- [x] 点击模态框外部区域可以关闭
- [x] 按 ESC 键可以关闭模态框

## 添加新想法功能（动态交互）
- [x] 页面有"Add New Idea"按钮，使用 `.btn-primary` 渐变橙色样式
- [x] 点击按钮打开添加表单模态框（样式与详情模态框一致）
- [x] 表单包含标题和内容输入框
- [x] 输入框使用 `var(--radius-md)` 圆角
- [x] 输入框聚焦时有橙色边框（`var(--color-primary-500)`）
- [x] 提交按钮使用主站 `.btn-primary` 样式，悬停有上浮效果
- [x] 可以提交新想法
- [x] 提交后新卡片立即显示在页面上，带有 `.reveal` 渐入动画
- [x] 表单有基本的验证（必填字段），错误时有视觉提示
- [x] 提交成功/失败有提示信息

## 数据持久化
- [x] 刷新页面后，添加的新想法仍然存在
- [x] 数据保存在 localStorage 中
- [x] 默认数据和本地数据正确合并

## 响应式设计
- [x] 页面在桌面端（>1024px）显示正常（3 列网格）
- [x] 页面在平板端（768px-1024px）显示正常（2 列网格）
- [x] 页面在移动端（<768px）显示正常（1 列网格）
- [x] 模态框在各种屏幕尺寸下都能正常工作
- [x] 移动端模态框有适配的样式（宽度、内边距调整）
- [x] 所有交互效果在移动端依然流畅

## 代码质量
- [x] 代码符合 ESLint 规范（运行 `npm run lint` 无错误）
- [x] 没有未使用的导入和变量
- [x] 组件结构清晰，职责分离
- [x] 样式 100% 使用 CSS 变量（`var(--xxx)`），符合设计系统
- [x] 复用主站已有的 CSS 类（`.card`, `.btn`, `.reveal`, `.stagger` 等）
- [x] 没有硬编码的颜色值、尺寸值

## 用户体验（动态交互效果）
- [x] 页面加载流畅，使用 `.reveal` 和 `.stagger` 动画渐进显示
- [x] 所有按钮悬停有颜色/位置变化
- [x] 所有卡片悬停有上浮 + 阴影 + 涟漪效果
- [x] 所有点击操作有 `scale(0.95)` 或类似的视觉反馈
- [x] 模态框打开/关闭有平滑的缩放 + 淡入淡出动画
- [x] 导航清晰，用户可以轻松返回首页
- [x] 表单提交有成功/失败提示
- [x] 所有过渡动画使用统一的缓动函数（`cubic-bezier(0.4, 0, 0.2, 1)` 或 `cubic-bezier(0.34, 1.56, 0.64, 1)`）
